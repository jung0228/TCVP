# Config examples

- **channel_categories.example.csv**: Example channel/category file for `find_popular_videos.py --batch`.
- Use `csv/channel_categories.csv` at the project root for real data.

Example categories: `sport`, `gaming`, `comedy`, `entertainment`, `talk show`, `podcast`, `making`
